{
  "scripts": {
    "dev": "tailwindcss -i src/input.css -o src/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
